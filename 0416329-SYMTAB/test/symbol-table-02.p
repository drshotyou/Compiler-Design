program foo(input, output, error);
    var a,b,c : integer;
    var d,e : real;
    var f : string;

    function func1 : integer;
        var a : integer;
        var d : real;
        var f : string;
        begin
        end;

    procedure proc1(g : array [1 .. 10] of integer);
        var a : integer;
        var d : real;
        var f : string;
        begin
        end;

    begin
    end.
